## 1. 项目基础设施

- [x] 1.1 安装 `react-markdown` 和 `remark-gfm` 依赖
- [x] 1.2 创建 `apps/web/components/chat/` 目录
- [x] 1.3 创建 `apps/web/config/mock-chat.ts` 假数据文件（预设会话、模拟 AI 响应函数、数据类型定义）

## 2. 核心组件实现

- [ ] 2.1 实现 `ChatPage` 页面容器组件（管理会话状态、当前会话 ID、消息列表）
- [ ] 2.2 实现 `SessionList` 会话列表侧栏组件（会话项渲染、新建按钮、删除按钮、活跃高亮）
- [ ] 2.3 实现 `MessageArea` 消息展示区组件（滚动到底部、消息列表渲染）
- [x] 2.4 实现 `MessageBubble` 消息气泡组件（用户/助手样式区分、Markdown 渲染）
- [x] 2.5 实现 `ToolCallStatus` 工具调用状态组件（spinner 加载中、完成结果）
- [x] 2.6 实现 `ChatInput` 输入框组件（textarea 自适应高度、Enter 发送、Shift+Enter 换行、发送按钮）

## 3. 交互逻辑

- [ ] 3.1 实现会话切换逻辑（点击会话项切换消息内容）
- [ ] 3.2 实现新建会话逻辑（创建空会话、自动切换）
- [ ] 3.3 实现删除会话逻辑（移除会话、自动切换到第一个会话）
- [ ] 3.4 实现发送消息逻辑（添加用户消息、触发模拟 AI 响应）
- [ ] 3.5 实现流式模拟响应（逐字打字效果、工具调用状态先展示 spinner 再展示结果）

## 4. 页面集成

- [ ] 4.1 改造 `apps/web/app/page.tsx` 为聊天页面（替换欢迎信息，引入 ChatPage 组件）
- [ ] 4.2 补充 i18n 翻译键（会话列表标题、新建会话、输入占位符、发送按钮等）

## 5. 验证

- [ ] 5.1 运行构建和测试确认通过
